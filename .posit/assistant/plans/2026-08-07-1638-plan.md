# Plan: Fix `npm install` repo-root failure

## Goal
Resolve the `npm install` failure caused by running npm in `c:/Users/80010008/git/dreamleague` when the actual Node project lives in `web/`.

## What I found
- The repository root is an R project, not a Node package root.
- The root directory does **not** contain a `package.json`.
- The frontend app does contain a valid Node manifest at `web/package.json`.
- `web/README.md` already instructs users to run `npm install` and `npm run dev` from `web/`.
- The root-level `package-lock.json` appears orphaned and may be confusing users.

## Proposed approach
1. Confirm whether the root-level `package-lock.json` is intentional or leftover.
2. Decide whether to:
   - remove the orphaned root lockfile, or
   - leave it in place and document that npm must be run from `web/`.
3. If needed, update repository guidance so the correct install directory is obvious.
4. Verify the fix by checking that the documented workflow points to `web/` and that no root-level npm install is expected.

## Risks and constraints
- Do not add a root `package.json` unless the repository is meant to become a monorepo with a top-level Node workspace.
- Avoid touching the R app unless the change is required to clarify the install workflow.
- If the root lockfile is removed, confirm nothing depends on it first.

## Clarifying question
Should the fix be documentation-only, or do you want the orphaned root `package-lock.json` removed as part of cleanup?
